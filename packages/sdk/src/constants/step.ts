export enum ChannelStepType {
    Email = 'email',
    Sms = 'sms',
    Push = 'push',
    Chat = 'chat',
}

export enum ActionStepType {
    Digest = 'digest',
    Delay = 'delay',
    Custom = 'custom'
}